# Never commit secrets or local junk
.env
.env.*
!.env.example
node_modules/
.cache/
*.log
*.pem
*.key
*.mp4
*.wav
cookies.txt
gcs-credentials.json
# Ops / internal — not for public host
admin/
**/admin/
# Source maps embed originals — keep local only, never push to public GitHub/CDN
_review/
